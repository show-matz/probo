# probo

　**probo** （ぷろーぼ）は、 `bash` シェル上で動作する簡素なテキスト主義のテスト
管理ツールです。基本的に `bash` と `gnuplot` があれば動作します。 `gnuplot` が
ない場合はレポート生成機能で Burndown chart やサマリのグラフイメージの出力が
できませんが、生成されるレポートデータを使用して表計算ソフトでグラフを作成すること
ができます。

　マニュアルは manual/probo.html として用意してあります。詳細はそちらを参照して
ください。

