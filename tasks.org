# +STARTUP: overview
# +STARTUP: showeverything
#+STARTUP: content

* tasks [46/47]
** DONE conf file が存在しない場合のエラー処理を追加
** DONE probo.sh --track   でケースファイルに新規情報追加

    probo.sh --track [-d DESCRIPTION] STATUS RUNFILE...

    STATUS := READY|BLOCK|PASS|FAIL|RUN

** DONE probo.sh --log で指定ケースの track log をダンプ

    probo.sh --log RUNFILE...

** DONE --track / --log では conf の存在チェックや取り込みをせずに動作させる
** DONE --ls でカレントディレクトリの run file のステータスを一覧する
** DONE ターゲットとして、 markdown|raw
** DONE 画像ファイル名に２つ以上ピリオドを使うとうまく動かない問題に対処する
** DONE 名称を probo に変更する
** DONE GitLab 向けのレポートおよびステータス一覧を生成（して .md に挿入）する機能を追加
** DONE 別 markdown ファイルにしちゃうか、あるいは挿入かは要検討
** DONE ステータス一覧は full と pickup（READY/PASS 除外）
** DONE グラフの色指定を conf file で可能にしたい
** DONE --help で show-usage
** DONE probo.sh から拡張子を除去する
** DONE 差し込みで位置マーカーコメントがなかった場合ファイル末尾に位置マーカーごと追加
** DONE --track で -d で指定する description に空白文字を入れられない問題を修正
** DONE --log / --track で runfile name でなく case ID（数値）も受け付ける
** DONE --addcase でテストケースを追加する

	* testcase.md と testrun.md を追加してあげるくらいか。
	* PROBO_CASE_TEMPLATE 変数で初期化してあげるとか。
	* conf file が必要ってことにするの？
	* カレントまたはひとつ上のディレクトリで .case.template を探す、にするか
	* probo --addcase [COUNT]

** DONE --track で status を小文字で指定しても良いようにする
** DONE --addcase で template file 内のパラメータ展開 : ID とか group ？
** DONE PROBO_GROUPS 指定がない場合、カレント配下のディレクトリを走査する
	* この場合、.git のように . で始まるディレクトリは対象外に。
** DONE PROBO_SCHEDULE を作るのが面倒臭い

	* 日毎に消化予定数を指定するのはやりすぎ
	* 開始日付、目標消化日数、終了日付くらいの指定で良いか
	* PROBO_SCHEDULE を省略した場合にだけ、新規追加した変数３つから自動生成するようにした

** DONE --track に -f オプションを追加し、status チェックをバイパスする
** DONE 生成画像の背景色を指定可能にしたい
** DONE 設定変数名称を統一したい

	* PROBO_ で始めることにする？

** DONE markdown target でも指定があれば Burndown data を生成する
** DONE --init でなんか初期化する

	* やるとすれば、probo.conf を自動で生成してくれる‥‥‥くらい？

	* probo --init [-c CONF_FILE] REPORT_TYPE [GROUP...]

** DONE --report の処理時間が長いので状況通知をする

	* ただし -q オプションで黙るようにしとくか。

** DONE group 別のレポート生成もする？

	* これは基本的にやらない方向かな‥‥‥
	* やってもいい気がしてきた。
	* ステータス一覧（full/part）と、サマリの円グラフかな
	* これらは各ディレクトリ毎なので、ファイル名指定はしない（全体と同じにする）で、group 別生成要否だけフラグで与える？

** DONE 途中で実施対象外としたもののステータス REJECT を追加する

	* 終了扱いってことで

** DONE --addgrp でグループを追加する

	* probo --addgrp GROUP_NAME...
	* PROBO_GROUPS への追加は「自分で頑張ってね」とする
	* .group.readme.template があるなら README.md を生成する
	* case 追加は別途 probo --addcase してね、とする
	* 追加するファイル名は設定で渡すの？ ⇒ GitHub でも README.md だからひとまずいいのでは。

** DONE --init でのグループディレクトリ追加をとりやめる

	* .group.readme.template を生成するように変更する予定なので、
	* 生成した上記ファイルを修正してから probo --addgrp してもらう方針

** DONE --init で各種 .case.template ファイルを生成する

	* .case.template

** DONE --init で各種 .group.readme.template ファイルを生成する

	* .group.readme.template
	* REPORT_TYPE として gitlab とかを指定可能にする

** DONE PROBO_GROUPS 変数の廃止検討

	* .git みたいなドットで始まるディレクトリは除外
	* 上記に加えて、配下に *.testcase.md が存在しないディレクトリも除外
	* ‥‥‥で、良くない？
	* 問題は、「レポートでの表示順序制御」かな。
		* undocumented な挙動として PROBO_GROUPS を残すのはアリかも

** DONE --find で特定ステータスのケースを検索、って有用かな？ ⇒ --ls の拡張へ

	* report 生成すればいいんだけど、それが面倒な時に。
	* 汎用的な検索は grep 使いやがれ、でいいと思う。
	* 結局、--ls を groups のひとつ上のディレクトリでも実行できるようにした。
		* でもこれけっこう遅い‥‥‥。

** DONE --report で FAIL が一件もないと burndown image 生成できない

　以下で直った。

#+BEGIN_SRC diff
@@ -340,6 +340,9 @@ function probo-make-burndown-image {
     local TOP_Y2=$(cat $DAT_FILE | cut -d, -f4 | sort -g | tail -1)    # Fail
     TOP_Y1=$((TOP_Y1 + 10))
     TOP_Y2=$((TOP_Y2 * 10))
+    if [ $TOP_Y2 -eq 0 ]; then
+        TOP_Y2=10
+    fi
     # gnuplot 設定ファイルを作成
     probo-make-burndown-gpfile "$$.tmp.gp" "$DAT_FILE" ${IMG_TYPE} $TOP_Y1 $TOP_Y2
     gnuplot "$$.tmp.gp" > "${OUT_FILE}"
#+END_SRC

** DONE --edit で runfile とか casefile を編集する

	* probo --edit [-c] FILE-OR-NUMBER
		* -c で case file、なければ run file
	* PROBO_EDITOR 変数が必要になるね。
		* PROBO_EDITOR 変数が空ならどこにでもある vi にする？
		* vi ならブロックだろうけど、emacsclient なら & つけたいな
		* PROBO_EDITOR="emacsclient %1 &" みたいに定義させるか
		* よく考えてみたら conf file に依存できないだろこれ
		* これは .bashrc で定義してもらうことに
	* 動くようにはなった。あとはマニュアル。

** DONE --track -e で編集も可能にする

	* ただし、編集したのが 1 file だった場合のみね。
	* 動くようにはなった。あとはマニュアル。

** DONE --ls が遅い
** DONE --log が遅い
** DONE --report が遅い ⇒ まだ改善の余地はあるかもだけど 17sec->11sec の改善
** DONE --init での conf file 生成をもっと細かく制御する

	* 以下の３通りかな
		* raw
		* github/gitlab
		* turnup

** DONE --init で README.md ファイルを生成する ⇒ 一応やった

	* probo --init git(hub|lab) の場合かな
	* probo --init turnup の場合はどうする？  report.md とか？

** DONE testcase.md ファイルからテストを自動実行するような仕組みを検討する

	* それは probo の埒外な気もするが。
	* testcase.md に以下のような記述があれば良い気が。

<!-- probo test script : begin -->
```sh
# ここにテストスクリプトを記述する
# 最後は exit 0 または exit 1 してもらう（ゼロなら PASS、それ以外は FAIL）
```
<!-- probo test script : end -->

	* probo --run NUMBER かな
		* 内部で probo --track run NUMBER してくれる
		* 上記の内容が見付からなければエラー
		* あれば取出して bash で実行。
		* 標準出力はリダイレクトして runfile に転帰してくれる
		* $? で結果も見て probo --track (pass|fail) NUMBER してくれる

** DONE PASS が複数ある場合の扱いを考える

	* 「最後の FAIL」以降で「最初の PASS」を唯一の PASS として数える、かな
	* しかし、ケース更新とかで READY に戻ってるとかは？ ⇒ やはり「最後が PASS」かどうかだけでよい？
	* やはり、以下のルールかな
		* 「最後（つまり現在）のステータス」が PASS のものだけを PASS と数える
		* 当然、そのテストケースが PASS したのは最後の PASS の日付
		* だから、burndown chart での扱いは --ls のイメージで得た一覧で日付別の PASS 数を数えれば良い
		* これに対して、FAIL は日付別の発生数を調べるので従来通り

** TODO マニュアルを書け

	* 今やってます‥‥‥

